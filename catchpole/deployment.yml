apiVersion: apps/v1
kind: Deployment
metadata:
  name: catchpole
  labels:
    app: catchpole
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: catchpole
  template:
    metadata:
      labels:
        app: catchpole
    spec:
      containers:
        - name: catchpole
          image: ghcr.io/fireacademy/catchpole:1.2.8
          env:
            - name: CATCHPOLE_CONFIG
              valueFrom:
                secretKeyRef:
                  name: catchpole
                  key: CATCHPOLE_CONFIG
            - name: CATCHPOLE_LISTEN_PORT
              valueFrom:
                secretKeyRef:
                  name: catchpole
                  key: CATCHPOLE_LISTEN_PORT
            - name: GOLDEN_GATE_ADDRESS
              valueFrom:
                secretKeyRef:
                  name: catchpole
                  key: GOLDEN_GATE_ADDRESS
            - name: REDIS_CONNECTION_STRING
              valueFrom:
                secretKeyRef:
                  name: redis
                  key: REDIS_CONNECTION_STRING
          ports:
            - name: http
              containerPort: 5000
          resources:
            requests:
              cpu: "200m"
              memory: "400Mi"
            limits:
              cpu: "200m"
              memory: "400Mi"
          startupProbe:
            httpGet:
              path: /
              port: 5000
              scheme: HTTP
            initialDelaySeconds: 1
            failureThreshold: 3 
            periodSeconds: 5
